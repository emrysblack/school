// test for atoi
#include <iostream>
#include <string>
using namespace std;

int main(int argc, char** argv)
{
   char a;
   char b;
   string d = "Test";
   if (argc == 2)
   {
      d = argv[1];
   }
   cout << "Enter a letter: ";
   cin >> a;
   cout << "Enter another letter: ";
   cin >> b;
   int r = a - 'a';
   int c = b - 'a';
   cout << "r: " << r << " c: " << c << " d: " << d << endl;
   return 0;
}
